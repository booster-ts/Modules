{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test:unit",
    "install": "lerna run setup",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  }
}
