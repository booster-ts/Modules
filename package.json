{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test:unit && lerna run test:func",
    "install": "lerna run setup",
    "build": "lerna run build",
    "lerna:publish:beta": "lerna publish from-git --yes --npm-tag beta",
    "lerna:publish:latest": "lerna publish from-git --yes --npm-tag latest"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  }
}
